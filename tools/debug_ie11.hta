<!DOCTYPE html>
<meta charset="utf-8"/>
<meta http-equiv="x-ua-compatible" content="ie=11"/>
<title>debug ie11</title>
<style>
	body {
		margin: 0;
		background-color: magenta;
		font-family: "Segoe UI";
	}
	main {
		display: inline-block;
		background-color: white;
		padding: 1rem;
	}
	h1 {
		margin: 0;
		font-size: 1.5rem;
	}
	dl {
		margin: 0;
		margin-top: 1rem;
	}
	dt {
		font-style: italic;
	}
	dd {
		white-space: pre;
		font-weight: bold;
	}
</style>
<script>
	function add(key, value) {
		const dl = document.querySelector("dl");
		const dt = document.createElement("dt");
		const dd = document.createElement("dd");
		dt.textContent = key;
		dd.textContent = value;
		dl.appendChild(dt);
		dl.appendChild(dd);
	} 
	document.addEventListener("DOMContentLoaded", function() {
		add("document.characterSet", document.characterSet); // utf-8
		add("document.compatMode", document.compatMode); // CSS1Compat
		add("document.documentMode", document.documentMode); // 11
		add("document.title", document.title); // debug ie11
		add("navigator.language", navigator.language); // de-DE
		// add("navigator.userAgent", navigator.userAgent);
		add("screen.availWidth", screen.availWidth);
		add("screen.availHeight", screen.availHeight);
	});
	addEventListener("load", function() {
		const main = document.querySelector("main");
		const rect = main.getBoundingClientRect();
		const width = rect.width + 22 /*padding*/;
		const height = rect.height + 56 /*padding*/;
		resizeTo(width, height);
		moveTo(
			(screen.availWidth / 2) - (width / 2),
			(screen.availHeight / 2) - (height / 2)
		);
	});
</script>
<main>
	<h1>debug ie11</h1>
	<dl></dl>
</main>
